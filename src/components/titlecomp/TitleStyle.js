import { StyleSheet } from "react-native";

export const titleStyle = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
    },
    title: {
        fontSize: 35,
        fontWeight: "bold",
        textAlign: "center",
        color: "#11224D",
    },
});
