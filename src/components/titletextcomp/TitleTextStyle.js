import { StyleSheet } from "react-native";

export const titleTextStyle = StyleSheet.create({
    container: {
        marginBottom: 10,
    },
    text: {
        fontSize: 16,
        color: "#11224D",
    },
});
